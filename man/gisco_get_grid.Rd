% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gisco-get-grid.R
\encoding{UTF-8}
\name{gisco_get_grid}
\alias{gisco_get_grid}
\title{Grid dataset}
\source{
\url{https://ec.europa.eu/eurostat/web/gisco/geodata/grids}.
}
\usage{
gisco_get_grid(
  resolution = c(100, 50, 20, 10, 5, 2, 1),
  spatialtype = c("REGION", "POINT"),
  cache_dir = NULL,
  update_cache = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{resolution}{The grid cell resolution in km. Available values are
\code{"1"}, \code{"2"}, \code{"5"}, \code{"10"}, \code{"20"}, \code{"50"} and \code{"100"}. See
\strong{Details}.}

\item{spatialtype}{A character string selecting \code{"REGION"} or \code{"POINT"}.}

\item{cache_dir}{A character string with a path to a cache directory. See
\strong{Caching strategies} section in \code{\link[=gisco_set_cache_dir]{gisco_set_cache_dir()}}.}

\item{update_cache}{A logical value indicating whether to refresh the
cached file. Defaults to \code{FALSE}. When set to \code{TRUE}, it forces a new
download.}

\item{verbose}{A logical value indicating whether to display informational
messages.}
}
\value{
A \code{\link[sf:st_sf]{sf}} object.
}
\description{
These datasets contain grid cells covering the EU and neighbouring countries
at resolutions from 1 km to 100 km. Population figures are available for
selected reference years.
}
\details{
\code{\link[=gisco_get_grid]{gisco_get_grid()}} downloads the GeoPackage representation of the grid as
polygon cells (\code{spatialtype = "REGION"}) or cell-centre points
(\code{spatialtype = "POINT"}). The official distribution also provides tabular
CSV and Parquet files, which this function does not download.

All grid geometries use \href{https://epsg.io/3035}{\code{EPSG:3035}}. Population
columns are named \code{TOT_P_YYYY}, where \code{YYYY} is the reference year. To
calculate population density, divide a population value by the cell area in
square kilometres (\code{resolution^2}).

The file sizes range from 428 KB (\code{resolution = 100})
to 1.7 GB (\code{resolution = 1}).
}
\section{Copyright}{
Population variables (\verb{TOT_P_*}) have year- and country-specific licensing
conditions. Other grid elements are covered by the general Eurostat
copyright provisions. Review the licensing table and metadata on the
official grid page before redistributing or publishing the data.
}

\examples{
\dontshow{if (gisco_check_access()) withAutoprint(\{ # examplesIf}
grid <- gisco_get_grid(resolution = 20)

# Proceed if downloaded correctly.

if (!is.null(grid)) {
  library(dplyr)

  grid <- grid |>
    mutate(popdens = TOT_P_2021 / 20^2)

  breaks <- c(0, 0.1, 100, 500, 1000, 5000, 10000, Inf)

  # Cut groups.
  grid <- grid |>
    mutate(popdens_cut = cut(popdens,
      breaks = breaks,
      include.lowest = TRUE
    ))

  cut_labs <- prettyNum(breaks, big.mark = " ")[-1]
  cut_labs[1] <- "0"
  cut_labs[7] <- "> 10 000"

  pal <- c("black", hcl.colors(length(breaks) - 2,
    palette = "Spectral",
    alpha = 0.9
  ))

  library(ggplot2)

  ggplot(grid) +
    geom_sf(aes(fill = popdens_cut), color = NA, linewidth = 0) +
    coord_sf(
      xlim = c(2500000, 7000000),
      ylim = c(1500000, 5200000)
    ) +
    scale_fill_manual(
      values = pal, na.value = "black",
      name = "people per square kilometer",
      labels = cut_labs,
      guide = guide_legend(
        direction = "horizontal",
        nrow = 1
      )
    ) +
    theme_void() +
    labs(
      title = "Population density in Europe (2021)",
      subtitle = "Grid: 20 km.",
      caption = paste(
        "Source: Eurostat GISCO grid dataset.",
        "Review the applicable population-data licence."
      )
    ) +
    theme(
      text = element_text(colour = "white"),
      plot.background = element_rect(fill = "grey2"),
      plot.title = element_text(hjust = 0.5),
      plot.subtitle = element_text(hjust = 0.5, face = "bold"),
      plot.caption = element_text(
        color = "grey60", hjust = 0.5, vjust = 0,
        margin = margin(t = 5, b = 10)
      ),
      legend.position = "bottom",
      legend.title.position = "top",
      legend.text.position = "bottom",
      legend.key.height = unit(0.5, "lines"),
      legend.key.width = unit(1, "lines")
    )
}
\dontshow{\}) # examplesIf}
}
\concept{grids}
